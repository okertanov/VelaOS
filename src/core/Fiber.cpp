/**
    @file       Fiber.cpp
    @brief      OS Fiber for the Tasks implementation

    @author     Oleg Kertanov <okertanov@gmail.com>
    @date       Oct 2011
    @copyright  Copyright (C) 2011 Oleg Kertanov <okertanov@gmail.com>
    @license    BSD
    @see LICENSE file
*/

#include "Platform.h"
#include "Fiber.h"

/*******************************************************************************
    Fiber
*******************************************************************************/
namespace Vela
{
    namespace Concurrent
    {

        /**
            @fn Vela::Concurrent::Fiber::Fiber()
            @brief

            @param[in]
            @param[out]
            @param[in,out]
            @return
        */
        Fiber::Fiber()
        {
        }

        /**
            @fn Vela::Concurrent::Fiber::~Fiber()
            @brief

            @param[in]
            @param[out]
            @param[in,out]
            @return
        */
        Fiber::~Fiber()
        {
        }

        /**
            @fn Vela::Concurrent::Fiber::Run()
            @brief

            @param[in]
            @param[out]
            @param[in,out]
            @return
        */
        void Fiber::Run()
        {
        }

        /**
            @fn Vela::Concurrent::Fiber::Yield()
            @brief

            @param[in]
            @param[out]
            @param[in,out]
            @return
        */
        void Fiber::Yield()
        {
        }

    }
}

